apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: todo-dev

resources:
  - ../../base

secretGenerator:
  - name: postgres-secret
    literals:
      - username=devuser
      - password=devpass123
generatorOptions:
  disableNameSuffixHash: true
